# 08 完整流程与技术方案 v1.0

整合 02 决策记录（D1–D30）、06 讨论稿 v0.4、07 商品目录。本文件是改原型的**唯一依据**。
日期：2026-09-11

---

## 0. 总览

### 0.1 一句话
路人被待机视频吸引 → 拍一张脸（可跳过）→ 选性别/身高/常穿尺码 → 形象在后台生成的同时挑衣服 → Create Look → 看到"自己"穿上的效果 → 扫码把结果、优惠券、购买入口带到手机 → 大屏自动清空。

### 0.2 主线
```
大屏：P0 待机视频 → P1 拍脸 → P2 身形信息 → P3 选款画廊 → P4 生成中 → P5 效果页 → P6 扫码交接 → P7 已带走/清空 → P0
                        └跳过┘            ↑形象后台生成（与 P3 并行）          ↓超时可提前扫码
手机：M1 我的试穿 → M2 商品详情 → 店内（叫店员）/ 线上（官网购买）→ M3 分享海报 / M4 删除记录
```
点击次数：P0 点屏 → P1 跳过 → P2 用默认值点 Create an avatar → P3 用默认主推款点 Create Look，最快 **4 次点击出图**；拍脸路径多 2 次（拍摄 + 使用这张）。

### 0.3 原型架构（纯静态，无后端）

| 项 | 方案 |
|---|---|
| 形态 | 纯 HTML/CSS/JS，无构建步骤；`index.html`（演示外壳：左大屏、右控制面板）、`kiosk.html`（大屏本体，也可单独全屏）、`m.html`（手机页，扫码真实可打开） |
| 画布 | 大屏固定 **1080×1920** 逻辑像素；外壳用 `transform: scale(min(vw/1080, vh/1920))` 等比缩放；真屏上缩放 = 1 |
| 状态 | 单一状态机 `state = { step, lang, face, profile, avatarStatus, selection, look, scenario, lastInteraction }`；每个页面一个 `render()`；只有状态变化才重渲染 |
| 数据 | `data/products.json`（16 件：id、分类、名称中英、演示价、官网链接、货架位置、库存、图片）、`data/looks.json`（6 套精选：单品 id 组合 → 效果图路径） |
| 部署 | GitHub Pages（静态）；二维码指向部署后的 `m.html` |
| 依赖 | 仅两个 CDN 库：二维码 `qrcode-generator`（cdnjs）、人脸检测 MediaPipe Tasks Vision（jsdelivr，Should 级，失败自动降级） |

### 0.4 生产方案（写进一页方案，不在原型实现）
大屏 Web 应用（Kiosk 模式浏览器）+ 手机 H5 + 后端：会话服务、生成任务队列（按门店限流）、GPU 推理（身体生成 + 人脸身份保持 + 2D 生成式试穿）、对象存储（24h 生命周期自动删除）、签名短链、埋点。生成目标：P50 ≤ 8s、P95 ≤ 15s。

---

## 1. 视觉规范：简约 · 时尚（替换 Codex 的荧光绿风格）

参考：WEARFITS 截图、Zara / COS 官网的编辑感。**让衣服和人成为主角，界面退后。**

| 项 | 规范 |
|---|---|
| 色彩 | 白 `#FFFFFF`、纸白 `#F6F5F2`（背景）、墨黑 `#111111`（文字/主按钮）、灰 `#8A8A8A`（次要文字）、细线 `#E2E0DB`；**不用彩色强调色**；仅错误提示用低饱和砖红 `#A33A2B` |
| 字体 | 英文/数字：`Helvetica Neue`, `Inter`, Arial；中文：`PingFang SC`, `Noto Sans SC`；标签全大写 + 字距 0.18em（如 `SELECT YOUR LOOK`） |
| 字号（1080 画布） | 标签 24px ｜ 正文 30px ｜ 按钮 32px ｜ 页面标题 56px ｜ 待机大标题 96px |
| 形状 | **直角矩形**，1–2px 黑色描边（参考 WEARFITS 的框线标题和按钮）；不用胶囊圆角、不用阴影、不用渐变 |
| 按钮 | 主按钮：黑底白字，高 120px，全宽或 ≥ 480px；次按钮：白底黑描边；文字按钮：下划线 |
| 选中态 | 黑色 2px 描边 + 右上角黑底白勾（照搬参考图 5） |
| 动效 | 只用淡入 200ms 和进度环；不做弹跳 |
| 图标 | 1.5px 线性图标（返回、相机、相册、勾、关闭），不用 emoji |

### 1.1 大屏安装与分区（55 寸竖屏，下沿离地约 70cm）

| 区域 | y 范围 | 离地 | 用途 |
|---|---|---|---|
| 顶栏 | 0–140 | 185–192cm | 返回、步骤点、语言、清除（低频操作） |
| 展示区 | 140–760 | 150–185cm | 摄像头画面、形象预览、效果图上半身（**只看不点**） |
| 触控区 | 760–1680 | 83–150cm | 所有选择和主按钮 |
| 底部留白 | 1680–1920 | 70–83cm | 不放可点元素；放隐私小字 |

触控目标 ≥ 96×96px（约 6cm）；相邻目标间距 ≥ 24px。

---

## 2. 大屏逐页

每页格式：目的 ｜ 布局 ｜ 元素与文案 ｜ 交互 ｜ 异常 ｜ 技术实现 ｜ 埋点

### P0 待机视频
- **目的**：吸引路人（界面新闻 2018："鲜有客人主动使用"→ 必须主动吸引）
- **布局**：全屏视频；底部触控区叠一层渐变遮罩 + 文案
- **元素**：大标题「See yourself in it. / 10 秒，看见自己穿上它」；按钮 `TAP TO START · 点击开始`；小字「No sign-in · Photo optional · Auto-cleared / 无需登录 · 拍照可选 · 离开即清除」；右上 `中 / EN`
- **交互**：点屏幕任意处 → P1；视频循环
- **异常**：视频加载失败 → 显示静态海报图（`poster` 属性）
- **技术**：`<video src="assets/attract.mp4" poster="assets/attract.jpg" autoplay muted loop playsinline>`（浏览器只允许静音自动播放）；视频规格 1080×1920、H.264、10–15s、≤15MB；关键画面避开下方 1/3。**你们制作的视频放入 `assets/attract.mp4` 即可替换占位**
- **埋点**：`attract_tap`

### P1 拍脸（参考图 1）
- **目的**：获取人脸以生成"像你"的形象；可跳过
- **进入时先弹同意层（首次）**：标题「Before we take a photo / 拍照前说明」；三条：只用于本次生成、不用于 AI 训练；只在本机处理，离开即删除；结果只通过你扫码带走。按钮：`AGREE & CONTINUE` ／ `SKIP, USE DEFAULT FACE`
- **布局**：顶部框线标题 `TAKE A PICTURE OF YOURSELF`；提示行「Good lighting · Face the camera · No hat or sunglasses / 光线充足 · 正对镜头 · 摘下帽子墨镜」；展示区为摄像头实时画面（镜像）+ 椭圆人脸框；触控区：圆形快门（直径 180px）、`Photo tips`；底部：`SKIP, USE DEFAULT FACE`、小字「Your photo is not used for AI training」
- **交互**：点快门 → 3 秒倒计时 → 定格 → `USE THIS PHOTO`（→ P2）／`RETAKE`
- **异常（D24）**：未检测到人脸「We can't see your face — step a little closer」；多张脸「Make sure only you are in the frame」；太暗「Find better lighting」；拒绝摄像头权限 / 无摄像头 → 自动改为模拟拍照
- **技术**：
  - `navigator.mediaDevices.getUserMedia({video:{facingMode:'user'}})`，画面只在本地 `<video>` 显示；拍摄用 `<canvas>` 截一帧存为内存中的 dataURL，**不上传、不写 localStorage**；清空时 `stream.getTracks().forEach(t=>t.stop())` 并置空
  - 人脸检测（Should）：MediaPipe Tasks Vision `FaceDetector`（WASM，端侧运行）数人脸个数与亮度，驱动上面三种提示；加载失败则不检测，直接允许拍摄
  - 演示面板提供「模拟：无人脸 / 多张脸 / 太暗」按钮，评审现场可复现
- **埋点**：`face_consent_agree` / `face_skip` / `face_capture` / `face_retake` / `face_error_{type}`

### P2 身形信息（参考图 3）
- **目的**：不问体重、不拍全身，只用性别、身高、常穿尺码生成身形（D18）
- **布局**：顶部标签 `SELECT SIZE`；框线说明条「We'll generate your body from your size — no weight, no full-body photo / 只用尺码生成身形，不问体重，不拍全身」；展示区：人脸缩略图（或默认脸图标）+ 已选摘要
- **元素**：`GENDER` [WOMAN][MAN]；`HEIGHT` 分档大按钮 [≤155][160][165][170][175][180+]（大屏上滑杆难拖，改分档）；`YOUR TYPICAL CLOTHING SIZE` [XS][S][M][L][XL][XXL][3XL]（两行，与参考图一致）；主按钮 `CREATE AN AVATAR`
- **默认值**：WOMAN / 165 / M（可直接点主按钮）
- **交互**：点 `CREATE AN AVATAR` → 立即进入 P3，形象**后台生成**（D22）
- **技术**：`state.profile = {gender, height, size}`；`startAvatarJob()` 用 `setTimeout` 模拟 6–10s，完成后 `avatarStatus = 'ready'`；尺码映射到素材：XS/S/M → M 图，L/XL/XXL/3XL → XL 图；性别选 MAN 时提示「本店为女装门店，已为你切换到替 TA 试模式」（演示素材只有女装）
- **埋点**：`profile_submit {gender,height,size,changedFromDefault}`

### P3 选款画廊（参考图 5）
- **布局**：顶部框线标题 `SELECT YOUR LOOK`，右上 `CLEAR (n)`；展示区：形象生成状态条「Creating your avatar… 60%」→「Avatar ready ✓」+ 已选单品平铺缩略图；触控区三行横滑：`TOPS`（5）、`BOTTOMS`（7）、`FULL BODY`（4），每行左右大箭头；卡片：商品图 + 名称 + `$49.90`
- **规则**：选 FULL BODY 时 TOPS/BOTTOMS 自动取消并半透明；选 TOPS/BOTTOMS 时 FULL BODY 取消；每行单选；默认预选 L1 主推款（红色针织裙）
- **主按钮**：`CREATE LOOK`（形象未就绪时可点，进入 P4 后合并等待）；不做 Neutral 姿势
- **异常**：本店缺货的款标 `ONLINE ONLY`（仍可试）
- **技术**：数据来自 `products.json`；横滑用 CSS `scroll-snap`；选中组合在 `looks.json` 中查找 → 命中则有效果图，未命中标记为降级
- **埋点**：`item_select {id}` / `look_create {items, matchedLook}`

### P4 生成中
- **布局**：展示区大进度环（直径 320px，黑色细线）+ 百分比；标题 `CREATING YOUR LOOK`；「About 8 seconds / 预计 8 秒」；触控区：所选单品卡片轮播卖点（面料、搭配建议）
- **情景**（演示面板切换）：
  - 正常：8s 完成 → P5
  - 慢速：15s 时出现「Taking longer than usual — scan now and we'll send it to your phone」+ 二维码 → 用户可提前 P6
  - 失败：自动重试 1 次（进度环回到 0 显示 `RETRYING`）→ 仍失败 → P5 降级模式
  - 断网：立即进入 P5 降级模式 + 提示「Offline — showing product photos」
- **降级模式**（对比 WEARFITS 的红字技术报错 + START OVER）：不显示技术报错；已选保留；展示所选单品的官方商品图拼版 + 「Your try-on will be ready soon. Here are the product photos」；按钮 `TRY AGAIN`
- **技术**：`setInterval` 驱动进度；若形象还在后台生成，进度 = 两个任务合并；未命中精选组合的选择，按"失败"情景走降级
- **埋点**：`gen_start` / `gen_success {ms}` / `gen_retry` / `gen_fallback {reason}` / `gen_early_handoff`

### P5 效果页
- **布局**：展示区 + 触控区上半部为**效果大图**（1080 宽，约 1100px 高，人像顶到 y≈140）；左上小标签 `AI PREVIEW`；若为示例形象，右上小字「Demo avatar / 演示使用示例形象」
- **信息卡**（触控区下半）：单品列表（名称 · `$59.90` · 货架 `B-03` · 库存 `M in stock` / `Online only`）
- **操作**：`CHANGE ITEMS`（回 P3，保留形象）｜ `CHANGE SIZE`（回 P2）｜ 主按钮 `TAKE IT WITH YOU · 扫码带走 + 领券`
- **规则**：大屏只显示价格，**不放可点的官网链接**（D27）
- **技术**：图片路径 `assets/tryon/{look}_{M|XL}.png`（生产换为 CDN 签名 URL）；图片转 WebP、≤400KB；`loading="eager"` 预加载下一套
- **埋点**：`result_view {look}` / `result_change_items` / `result_change_size`

### P6 扫码交接
- **布局**：标题 `CONTINUE ON YOUR PHONE`；二维码 480×480；三条利益点：保存试穿图 ｜ 领 $10 门店券 ｜ 叫店员送到试衣间；小字「Private link · expires in 24h」；兜底「No phone? Show this code to staff: A-1842」；按钮 `DONE, CLEAR MY SESSION`
- **技术（原型）**：二维码内容 = 部署后的 `m.html?l=L4&s=M&lang=en&sid=A1842&exp=<时间戳>`；**URL 里只有商品组合和尺码，不含人脸**；`qrcode-generator` 前端生成；演示外壳中另有「模拟扫码」直接在右侧打开手机页
- **技术（生产）**：后端会话 + 一次性签名短链（24h 过期）；手机端扫码后大屏收到回执
- **埋点**：`qr_show` / `qr_scanned`（原型用"模拟扫码"触发）

### P7 已带走 / 清空
- 「Sent to your phone. This screen will clear in 5s」→ 清空全部状态（人脸 dataURL、摄像头流、选择）→ P0
- **全局空闲**：任意页面 60s 无操作 → 屏内弹层「Still there? Clearing in 10s」+ `I'M STILL HERE`；10s 后清空（替换 Codex 的浏览器 `confirm()`）
- **全局清除**：顶栏 `CLEAR` → 屏内确认层 → 清空

---

## 3. 手机逐页（`m.html`，免登录）

### M1 我的试穿
- 顶部：品牌名 + 「Private · expires in 24h / 私人链接 · 24 小时后失效」
- 效果图（带脸，仅本人可见，D23）；多套时左右滑
- 门店券卡片「$10 off in store today」（今日有效）
- 单品列表：名称 · 价格 · `VIEW` → M2
- 底部：`SAVE IMAGE` ｜ `SHARE POSTER` ｜ `DELETE THIS TRY-ON`
- 若大屏仍在生成：显示「Still creating your look…」并定时刷新（原型模拟 5s 后出图）

### M2 商品详情
- 大图（官方商品图）、名称、价格、材质、尺码建议「You chose M — we suggest M」、本店库存、货架位置
- 按钮层级（D27）：
  - 主：`ASK STAFF TO BRING SIZE M TO A FITTING ROOM` → 生成取衣单号「#A18」
  - 次：`BUY ONLINE ↗`（每个商品每页仅 1 次）→ 官网链接，追加 `?utm_source=kiosk&utm_medium=tryon&store=SG01`，**不带任何个人信息**
  - 本店缺货（如 L1 红裙）：`BUY ONLINE` 升为主按钮，叫店员隐藏
- 页脚：演示脚注（D28）

### M3 分享海报
- 前端 `<canvas>` 合成 1080×1920：效果图 + 单品名/价格 + 门店名 + 指向**商品页**的二维码
- **默认不带脸**：切换「Include my face」才用效果图，否则用官方商品图拼版（D23）
- 输出：`navigator.share({files:[png]})`；不支持时显示图片 + 「长按保存」

### M4 删除记录
- 确认后清除本页数据 → 「Deleted. We don't keep your photo」；原型中同时使该链接失效（标记在 sessionStorage）

---

## 4. 演示外壳（`index.html`，给评委看的）

- 左：大屏按窗口高度等比缩放，外框极细
- 右：控制面板
  - 生成情景：正常 / 慢速 / 失败 / 断网
  - 拍脸情景：正常 / 无人脸 / 多张脸 / 太暗
  - 工具：从头演示、模拟扫码（右侧弹出手机页）、快进空闲计时、全屏
  - 当前旅程：P0–P7 高亮
  - 埋点流：实时显示最近 8 条事件（体现数据思维）
  - 脚注：商品图片与价格来源说明（D28）

---

## 5. 相对 Codex v0.1 的改动清单

| 类别 | 改动 |
|---|---|
| 必改 | 画布改为真实 1080×1920 + 等比缩放（原为 373px 宽、字号 10–20px） |
| 必改 | P0 改为视频位 |
| 必改 | 流程改为 拍脸 → 身形信息 → 选款画廊（原为预设形象卡片 → 服装网格） |
| 必改 | 选款改为 Tops / Bottoms / Full body 三行 + 互斥规则，按"一套"生成（原为 3 件分别生成） |
| 必改 | 真实二维码 + 独立 `m.html`（原为随机方块 + 弹窗） |
| 必改 | 60s 空闲清空 + 屏内确认层（原为无空闲清空、用 `confirm()`） |
| 必改 | 形象后台生成与选款并行 |
| 必改 | 效果图换为 gpt-image-2 生成素材（原为 CSS 色块人形） |
| 必改 | 视觉改为简约时尚：黑白、直角、框线（原为荧光绿、胶囊按钮） |
| 新增 | 真实商品图、美元演示价、官网链接（仅手机端）、缺货逻辑 |
| 新增 | 摄像头预览 + 端侧人脸检测（Should）+ 拍脸异常 |
| 新增 | 手机端分享海报（Canvas）、删除记录 |
| 新增 | 演示面板：拍脸情景、埋点流、脚注 |
| 删除 | 换颜色、姿势（Neutral）、生成页荧光色、预设形象卡片 |

## 6. 素材清单

| 素材 | 路径 | 状态 |
|---|---|---|
| 待机视频 | `assets/attract.mp4` + `attract.jpg` | 你们制作；先用基础形象图做占位海报 |
| 商品图 16 张 | `assets/products/`（从 `上衣&下装/` 复制；T5、B7 统一为浅灰底竖图） | 待处理 |
| 基础形象 M / XL | `assets/tryon/base_M.png`、`base_XL.png` | ✅ 已生成 |
| 试穿图 6 套 × 2 | `assets/tryon/L1–L6_{M,XL}.png` | L4_M ✅，其余生成中 |
| 生成提示词记录 | `tools/prompts_log.md` | 自动记录 |
