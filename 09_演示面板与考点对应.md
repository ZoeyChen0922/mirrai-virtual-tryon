# 09 演示面板与考点对应（写进文字方案，不放在原型页面上）

原型页面左侧是 1080 × 1920 竖屏大屏，按真实用户旅程点击；右侧是给评审用的演示面板，用来切换异常情景。页面上只保留按钮，每组按钮对应的考点写在这里。

> 更新：生成情景、拍脸情景两组切换按钮已从页面移除（用户决定：边界情况用文字说明即可），对应内容见文末「边界情况」。原型中相关逻辑仍保留：真实的人脸检测、生成失败自动降级等照常生效。

| 面板分组 | 对应题目考点 | 评审可以看到什么 |
|---|---|---|
| 从头演示 | 核心流程 | 回到待机，重走 P0–P7 |
| 模拟扫码 · 打开手机页 | 转化路径（大屏 → 手机） | 评审电脑无法真实扫码，点击后在右侧打开手机页：领券、叫店员送尺码、官网购买（缺货时升为主按钮）、分享海报、删除记录 |
| 快进 60 秒无操作 | 隐私保护（离开即清除） | 立即弹出「还在吗？」，10 秒后清空照片、尺码和已选 |
| 大屏全屏 | 真实尺寸 | 以 9:16 竖屏全屏查看 |
| 当前旅程 P0–P7 | 核心流程 | 实时显示用户走到哪一步 |
| 埋点事件（默认折叠） | 数据与转化思维 | 展开可见每一步的事件，如 face_skip、gen_fallback、rec_select、qr_scanned，可推出漏斗：使用 → 生成 → 扫码 → 叫店员 / 下单 |
| 服务状态提示（仅出错时显示） | —— | 开发用：生成服务的商品表与网页不一致时提示重启 |

页面上一句话说明：按真实旅程点击左侧大屏；右侧为演示工具。

## 边界情况（写进一页方案的"异常及隐私处理"）

**生成等待与失败**

| 情况 | 处理 |
|---|---|
| 正常 | 进度环 + 预计时长（约 30–60 秒），等待时展示单品卖点和 3 套推荐搭配 |
| 慢速（超过 15 秒） | 出现「不想等？先扫码」二维码，生成完成后自动出现在手机上，用户不必干站着 |
| 失败 | 自动重试 1 次；仍失败则降级为官方商品图，已选衣服保留，不显示技术报错（对比 WEARFITS 红字报错 + 从头再来） |
| 断网 | 直接展示本地缓存的商品图，提示可记下货号找店员 |
| 服务端不认识的商品 | 返回错误并走降级页，不生成错误结果冒充 |

**拍脸（隐私与门槛）**

| 情况 | 处理 |
|---|---|
| 拍照前 | 单独同意说明；可一键跳过，改用默认形象 |
| 没检测到人脸 | 提示「没有看到你的脸，请靠近一点」，快门不可点 |
| 画面里多张脸 | 提示「请确保画面里只有你一个人」（商场身后常有路人） |
| 光线太暗 | 提示「光线太暗，请换个亮一点的位置」 |
| 无摄像头 / 拒绝授权 | 自动改为跳过拍照的流程 |
| 离开或 60 秒无操作 | 屏幕内提示「还在吗？」，10 秒后清空照片、尺码和已选 |
