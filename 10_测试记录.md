# 10 用户旅程测试记录

日期：2026-09-10
环境：`tools/server.py`（http://localhost:4174，gpt-image-2 已连接）；Claude 内置浏览器；测试通过脚本点击页面元素完成
限制：内置浏览器禁止调用摄像头，所以拍照走"模拟拍照"；真实摄像头与 MediaPipe 人脸检测需要在 Chrome 中手动验证

## 结果汇总：全部通过（发现并修复 1 个问题）

| 模块 | 用例 | 结果 |
|---|---|---|
| **服务端** | 商品表 16 件 / 8 套与前端一致 | ✅ |
| | 不认识的商品 → 409 错误，不生成错误图 | ✅ |
| | 默认身形 + 精选搭配 + 未拍脸 → 直接用预生成图，不调用接口 | ✅ |
| | 真实生成：人脸 + 精选搭配 + XL + 170cm | ✅ 28 秒 |
| | 用正确令牌取结果 → ready；错误令牌 → none；删除后 → none | ✅ |
| **P0 待机** | 待机页显示；中英切换 | ✅ |
| **P1 拍脸** | 同意弹层；多张脸 / 无人脸 / 太暗 → 提示且快门不可点；恢复正常后可拍 | ✅ |
| | 拍摄 → 重拍 / 使用这张；跳过 | ✅ |
| **P2 身形** | 无性别选项；身高、尺码可选；返回键回到 P1 | ✅ |
| **P3 画廊** | 16 张卡片；默认选中 L1 | ✅ |
| | 连衣裙与上下装互斥；只选上装时按钮置灰并提示 | ✅ |
| | 清空；左右箭头滚动 | ✅（箭头已修复，见下） |
| **P4 生成** | 进度环；3 套推荐；点推荐切换搭配 | ✅ |
| | 慢速：15 秒前无二维码，15 秒后出现「先扫码」，继续生成至出结果 | ✅ |
| | 失败：显示「正在自动重试」→ 降级为商品图，无技术报错，提供「再试一次」 | ✅ |
| | 断网：降级为商品图，已选保留 | ✅ |
| **P5 效果页** | 效果图、单品价格和货架；换一套回到画廊并保留所选 | ✅ |
| **P6 扫码** | 二维码生成；模拟扫码打开手机页 | ✅ |
| **P7 清空** | 扫码后 5 秒自动回待机 | ✅ |
| **全局** | 60 秒空闲提示；「我还在」保留；10 秒无响应清空 | ✅ |
| | 清除 → 确认弹层；取消保留；确认清空 | ✅ |
| **手机页** | 结果图、24 小时私人链接提示、领券、单品列表 | ✅ |
| | 店内有货：主按钮叫店员，官网购买为次按钮（带门店和语言参数） | ✅ |
| | 仅线上（F9）：官网购买升为主按钮，隐藏叫店员 | ✅ |
| | 海报默认不带人像，可切换；分享不支持时提示长按保存 | ✅ |
| | 删除 → 已删除页；过期链接 → 已过期页 | ✅ |
| | 提前扫码：先显示「生成中」，出结果后自动显示 | ✅ |
| **官网页** | shop.html 显示商品、来源门店，语言跟随手机页 | ✅ |

## 发现并修复的问题

| 问题 | 原因 | 修复 |
|---|---|---|
| 非预生成组合无法生成 | 生成服务仍在运行 20:55 启动的旧代码，不认识新商品 | 重启服务；页面在商品表不一致时显示提示 |
| 画廊左右箭头不滚动 | 原生平滑滚动与横滑吸附冲突 | 改为自绘滚动动画，页面隐藏时直接跳转；吸附改为 proximity |

## 测试说明
- 情景切换消息是异步发送的，测试时需先等待约 0.5 秒再点击生成；真实演示中不存在这个问题。
- 页面隐藏超过数分钟后，浏览器会限制定时器和动画，影响计时类用例；上表结果均在排除这一干扰后重新验证。
